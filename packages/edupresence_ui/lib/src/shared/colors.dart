import 'package:flutter/material.dart';

/// Application colors
abstract class AppColors {
  /// Primary color
  static const primary = Color(0xFF1976D2);

  /// Secondary color
  static const secondary = Color(0xFF03DAC6);

  /// Error color
  static const error = Color(0xFFB00020);

  /// Success color
  static const success = Color(0xFF4CAF50);

  /// Warning color
  static const warning = Color(0xFFFFA000);

  /// Info color
  static const info = Color(0xFF2196F3);

  /// Background color (light)
  static const background = Color(0xFFF5F5F5);

  /// Background color (dark)
  static const backgroundDark = Color(0xFF121212);

  /// Surface color (light)
  static const surface = Color(0xFFFFFFFF);

  /// Surface color (dark)
  static const surfaceDark = Color(0xFF1E1E1E);

  /// Border color (light)
  static const border = Color(0xFFE0E0E0);

  /// Border color (dark)
  static const borderDark = Color(0xFF303030);

  /// Text primary color (light)
  static const textPrimary = Color(0xFF000000);

  /// Text primary color (dark)
  static const textPrimaryDark = Color(0xFFFFFFFF);

  /// Text secondary color (light)
  static const textSecondary = Color(0xFF666666);

  /// Text secondary color (dark)
  static const textSecondaryDark = Color(0xFFB3B3B3);

  /// Text hint color (light)
  static const textHint = Color(0xFF999999);

  /// Text hint color (dark)
  static const textHintDark = Color(0xFF808080);

  /// Disabled color (light)
  static const disabled = Color(0xFFE0E0E0);

  /// Disabled color (dark)
  static const disabledDark = Color(0xFF404040);

  /// Overlay color (light)
  static const overlay = Color(0x80000000);

  /// Overlay color (dark)
  static const overlayDark = Color(0x80FFFFFF);
}
